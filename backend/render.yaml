services:
  - type: web
    name: real-estate-api
    env: ruby
    rootDir: backend
    buildCommand: bundle install
    startCommand: bundle exec ruby app.rb
    envVars:
      - key: RAPIDAPI_KEY
        sync: false
      - key: ENVIRONMENT
        value: production 